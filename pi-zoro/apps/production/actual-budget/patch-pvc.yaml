apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: actual-budget-data-pvc
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: qnap-iscsi-manual
  volumeName: actual-budget-data-pv
  resources:
    requests:
      storage: 2Gi
