apiVersion: source.toolkit.fluxcd.io/v1
kind: HelmRepository
metadata:
  name: kube-prometheus-stack
  namespace: monitoring
spec:
  interval: 24h                                              # Sync the repo index every 24 hours
  url: https://prometheus-community.github.io/helm-charts

